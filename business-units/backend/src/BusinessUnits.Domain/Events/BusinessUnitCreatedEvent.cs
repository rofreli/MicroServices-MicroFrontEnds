namespace BusinessUnits.Domain.Events;

public sealed record BusinessUnitCreatedEvent(string BusinessUnitId) : IDomainEvent
{
    public Guid EventId { get; } = Guid.NewGuid();
    public DateTime OccurredAt { get; } = DateTime.UtcNow;
}

public sealed record BusinessUnitUpdatedEvent(string BusinessUnitId) : IDomainEvent
{
    public Guid EventId { get; } = Guid.NewGuid();
    public DateTime OccurredAt { get; } = DateTime.UtcNow;
}

public sealed record BusinessUnitDeletedEvent(string BusinessUnitId) : IDomainEvent
{
    public Guid EventId { get; } = Guid.NewGuid();
    public DateTime OccurredAt { get; } = DateTime.UtcNow;
}
